/**
 * Hook gen-component-demo — el demo GH Pages usa cdn/ ya versionado en el repo.
 * (tsc worker-swagger es opcional para publicar el demo estático.)
 */
console.log("front-shared demo build OK — cdn/ listo para GH Pages");
